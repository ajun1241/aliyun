<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.modcreater.tmdao.mapper.SafetyMapper">
    <insert id="insertRecord" parameterType="java.util.Map">
        insert into informationsafety(userId, createDate, operationType, networkSourceAddress, networkTargetAddress, appType, operationContent)
        values (#{userId},#{createDate},#{operationType},#{networkSourceAddress},#{networkTargetAddress},#{appType},#{operationContent})
    </insert>
</mapper>